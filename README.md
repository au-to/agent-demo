# agent-demo
Learning about agent dev.

## 环境准备

1. 安装依赖：
   ```bash
   python -m pip install -r requirements.txt
   ```
2. 配置环境变量（创建本地 `.env` 文件）：
   ```bash
   cp .env.example .env
   ```
3. 将 `.env` 中的 `DASHSCOPE_API_KEY` 替换为你的真实密钥。

## 运行

```bash
python chat.py
```
